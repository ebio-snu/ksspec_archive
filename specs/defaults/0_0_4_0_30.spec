{
  "Class" : "node",
  "Type" : "gateway/level1",
  "Model" : "DEF-GWNODE-001",
  "Name" : "디폴트 게이트웨이노드 (protocol 30)",
  "_comment" : "protocol 30 은 게이트웨이 자체 상태/제어 영역이 없다. 자체 영역이 없는 노드는 CommSpec 자체를 적지 않는다 — 자체 영역 크기가 0 이므로 자식 슬롯은 base 부터 바로 시작한다 (read=201, write=501). SPEC.md §10.2."
}
